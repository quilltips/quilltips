
import { Search } from "@/components/Search";
import { Layout } from "@/components/Layout";

const SearchPage = () => {
  return (
    <Layout>
      <Search />
    </Layout>
  );
};

export default SearchPage;
